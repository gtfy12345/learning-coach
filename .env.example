# 可选启动配置：Web 可以不创建 .env，并在本机设置页完成模型配置。
# Optional startup configuration. A .env file is not required for the Web app:
# start the service, then use the local settings page to test and apply an API
# model in process memory or select an authenticated Codex/Claude CLI. Values
# below remain useful for direct CLI runs and reproducible server startup.
# The settings page can also opt into writing the applied model selection and
# per-provider credentials back to this file (checkbox "同时保存到本机 .env"):
# ZHIPU_API_KEY / DEEPSEEK_API_KEY / DASHSCOPE_API_KEY / OPENAI_COMPATIBLE_API_KEY
# plus optional {PROVIDER}_BASE_URL overrides for the domestic providers.
# The settings page includes presets for DeepSeek、通义千问、智谱 GLM and a
# custom OpenAI-compatible option. Custom Base URLs must use HTTPS. These page
# values remain in process memory and are not written back to this file.
#
# The legacy MODEL_ID still works. CHAT_MODEL_ID controls diagnosis/teaching,
# while ASSESSMENT_MODEL_ID can use a different provider or model.
CHAT_MODEL_ID=openai:gpt-5-mini
# ASSESSMENT_MODEL_ID=anthropic:claude-sonnet-4-6

# Optional stronger teaching model. Context middleware selects it only when
# mastery is below 60 or at least two recent errors are active.
# ADVANCED_CHAT_MODEL_ID=openai:gpt-5.4

# Optional LCEL task fallbacks. If ASSESSMENT_FALLBACK_MODEL_ID is omitted,
# assessment inherits CHAT_FALLBACK_MODEL_ID. Empty values disable fallback.
# CHAT_FALLBACK_MODEL_ID=anthropic:claude-sonnet-4-6
# ASSESSMENT_FALLBACK_MODEL_ID=google_genai:gemini-2.5-flash-lite

# Official CLI login mode is selected by the model ID. It does not read the
# CLI token files and ignores provider API key variables in the child process.
# The local Web MVP can replace these settings from the settings page for new
# sessions only; it never exposes keys to the browser.
# CHAT_MODEL_ID=codex_cli:default
# CHAT_MODEL_ID=claude_code:sonnet
# CHAT_MODEL_ID=gemini_cli:default
# CLI_MODEL_TIMEOUT_SECONDS=300

# auto prefers provider-native JSON Schema, then tool calling. An authenticated
# Gemini CLI uses the explicit prompt_json fallback with Pydantic validation.
# Use native or tool to override incomplete profiles or compatible endpoints.
STRUCTURED_OUTPUT_STRATEGY=auto

# auto trusts the model profile. Use allow only when a compatible endpoint
# supports image input but does not publish profile metadata.
IMAGE_INPUT_POLICY=auto

# Hybrid RAG defaults to an offline deterministic feature-hash embedding.
# Set a LangChain provider:model only when external semantic embedding calls,
# their credentials, cost and data handling are acceptable.
EMBEDDING_MODEL_ID=local:hash-v1
# EMBEDDING_MODEL_ID=openai:text-embedding-3-small

# Provider credentials. Configure only the providers selected above.
OPENAI_API_KEY=replace-with-your-api-key
# ANTHROPIC_API_KEY=replace-with-your-api-key
# GOOGLE_API_KEY=replace-with-your-api-key

# For an OpenAI-compatible endpoint, keep the openai: prefix and set its URL.
# OPENAI_BASE_URL=https://example.com/v1

# Optional LangSmith tracing
LANGSMITH_TRACING=false
# LANGSMITH_API_KEY=replace-with-your-langsmith-api-key
# LANGSMITH_PROJECT=learning-coach

# Maximum duration for one Web graph run. The SSE endpoint emits a safe timeout
# event when this positive number of seconds is exceeded.
WEB_RUN_TIMEOUT_SECONDS=120

# Hard per-teaching-run budgets. Values are server-owned: browsers cannot raise
# them. Model calls: 1..10. Tool calls: 0..8 (0 disables teaching tools).
CONTEXT_MODEL_CALL_LIMIT=3
CONTEXT_TOOL_CALL_LIMIT=2

# Node-level result cache for the diagnostic stage. Sessions in the same
# process reuse the diagnostic question for an identical topic and image set;
# set to false to regenerate every time. The cache never persists to disk.
GRAPH_NODE_CACHE=true

# Durable execution. Empty (or "memory") keeps everything in process memory;
# point at a SQLite file to resume interrupted sessions across processes with
# the same --thread-id (CLI) or thread id.
CHECKPOINT_DB_PATH=
# Long-term learner memory store. Empty keeps the profile in process memory;
# a SQLite path persists it across restarts. Profiles are keyed by learner id.
# Chapter progress of material courses uses this same store; the book text
# itself stays in process memory only (re-upload the same file after a restart).
MEMORY_DB_PATH=
# Require explicit approval before learner code runs in the local restricted
# executor. Set to false to keep the pre-stage-11 direct execution behavior.
CODE_EXECUTION_APPROVAL=true
