# 2026-08-17 工作日志

## 00:30 工作记录

### 完成事项

- 修复 Codex CLI 0.144.1 移除旧审批参数后导致的模型调用失败，改用显式 `approval_policy="never"` 并保留只读 sandbox。
- 在 Codex Structured Outputs 边界递归补齐严格对象 Schema，覆盖根对象和嵌套对象回归测试。
- 完成 Follow-up 设计、计划、BUG-0002 与交付复盘；完整测试 317 项通过，真实 Web 创建会话返回 HTTP 201。

### 关联 Commit

- `fix(cli): restore Codex exec compatibility (BUG-0002)`

### 备注

- See [BUG-0002](../bugs/BUG-0002.md)。
- 服务以 `WEB_RUN_TIMEOUT_SECONDS=300` 和 `codex_cli:default` 重启，项目根目录没有 `.env`。

## 21:52 工作记录

### 完成事项

- 建立“先教后测 + 页面模型设置”后续设计与四阶段实施计划。
- 将新会话默认流程改为先讲解、再检查理解，同时保留显式 `diagnose_first` 兼容模式和旧检查点语义。
- 为 CLI、Web API、LangGraph 路由、理解检查及既有补救/审批恢复协议补充回归测试。

### 关联 Commit

- `feat(workflow): 接入先教后测学习流程`

### 备注

Auto-committed by /tdd phase-commit, plan: evaluation-security-delivery-follow-up-teach-first-model-settings

## 22:23 工作记录

### 完成事项

- 更新 README 与 `.env.example`，明确 `.env` 可选、先教后测默认流程、设置页操作、内存密钥和新会话生效边界。
- 完成 345 项全量测试、Python 编译、前端语法、计划上下文与文档索引验证。
- 生成先教后测与本地模型设置交付复盘，并将 spec、plan 和全部 checklist 同步为 completed。

### 关联 Commit

- `docs(delivery): 完成先教后测与模型设置交付`

### 备注

Auto-committed by /tdd phase-commit, plan: evaluation-security-delivery-follow-up-teach-first-model-settings

## 22:17 工作记录

### 完成事项

- 新增独立模型设置页，支持 API Provider/主评价模型选择、真实连接测试门禁，以及 Codex/Claude 官方 CLI 登录与应用。
- 首页增加学习模式选择并更新为“先教学、再理解检查”的默认文案、时间线和中断展示。
- 完成无浏览器持久化、错误脱敏、键盘标签、JavaScript 语法和真实浏览器布局验收。
- 修复无启动模型时设置页无法自举，以及应用配置后首页仍误判未配置的跨页面衔接问题。

### 关联 Commit

- `feat(web): 增加模型设置页与学习模式入口`

### 备注

Auto-committed by /tdd phase-commit, plan: evaluation-security-delivery-follow-up-teach-first-model-settings

## 22:07 工作记录

### 完成事项

- 实现进程内模型配置服务，API 配置必须先通过最小真实兼容性测试，再用 5 分钟有效的一次性票据应用。
- 将会话绑定到不可变的模型运行时版本，配置切换后同步回答、SSE 恢复与时间旅行仍使用原运行时。
- 增加仅限回环客户端、同源 JSON 写入的配置与 Codex/Claude 官方 CLI 认证 API，并覆盖密钥脱敏和失败原子性测试。

### 关联 Commit

- `feat(models): 增加内存模型配置与认证 API`

### 备注

Auto-committed by /tdd phase-commit, plan: evaluation-security-delivery-follow-up-teach-first-model-settings
