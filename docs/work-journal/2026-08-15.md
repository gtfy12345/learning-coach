# 2026-08-15 工作日志

## 00:55 工作记录

### 完成事项

- 完成 GraphRAG 与知识前置图的设计文档、实施计划、测试计划和可执行上下文声明。
- 明确默认离线实体关系抽取、显式可注入结构化模型增强、确定性别名消歧和有界 BFS。
- 锁定图与 Hybrid RAG 的 RRF 融合、前置知识解释、Web 概念图和无图安全降级边界。
- 明确概念图是运行时派生状态，不回写原始资料、Chunk 或增量索引。
- 使用项目虚拟环境验证计划上下文通过，并完成差异格式检查。

### 关联 Commit

- `docs(graphrag): 定义知识前置图交付计划`

### 备注

方案 A：默认确定性离线抽取，结构化模型抽取器仅由调用方显式注入。

## 01:01 工作记录

### 完成事项

- 定义有界概念节点、关系、前置路径和 GraphRAG 报告 Schema，并保持旧教学结果兼容。
- 实现中英文前置、组成和关联关系抽取，支持 Markdown 标题与代码标识符。
- 实现 Unicode/标识符规范化、显式别名归并与不确定同名隔离。
- 增加可注入结构化模型抽取器，非法输出、未知 Chunk 和模型异常均安全降级。
- 完成 9 项 GraphRAG 聚焦测试及 32 项 Hybrid RAG 回归、Python 编译和差异检查。

### 关联 Commit

- `feat(graphrag): 图 schema 与实体消歧`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph

## 01:09 工作记录

### 完成事项

- 按图距离、路径置信度和查询匹配对关系证据 Chunk 进行稳定排名。
- 使用 RRF 融合 Hybrid 与图排名，保留关键词、向量、重排、图分和最终融合分追溯。
- 实现 GraphStudyRetriever，先执行原自校正检索，再做图增强；无图或异常时保持 Hybrid 结果。
- 默认检索适配器启用离线 GraphRAG，仍允许注入旧 Hybrid Retriever。
- 旧粘贴文本的公开 Chunk ID 同步映射到来源、概念节点、关系与前置解释。
- 完成 64 项 GraphRAG、Hybrid RAG 和兼容检索测试。

### 关联 Commit

- `feat(graphrag): 融合图与 hybrid 检索`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph

## 01:05 工作记录

### 完成事项

- 构建稳定、去重且有边界的会话级概念图，合并关系证据与最高置信度并过滤自环。
- 从查询、学习薄弱信息和 Hybrid 命中 Chunk 识别图遍历种子。
- 实现深度 3、访问 24 节点、最多 5 路径的反向前置 BFS，覆盖链、分支与环。
- 生成包含目标、前置路径、薄弱点匹配、Chunk ID 和安全来源位置的补课解释。
- 完成 16 项 GraphRAG 聚焦测试及摄取、Hybrid RAG 相邻回归，共 60 项通过。

### 关联 Commit

- `feat(graphrag): 概念图构建与前置遍历`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph
