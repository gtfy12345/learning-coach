# 2026-08-15 工作日志

## 19:12 工作记录

### 完成事项

- 完成 README 的 Tool Calling、有界 ReAct、本地受限执行、Web 使用方式和非强沙箱边界说明，并增加公开文档契约测试。
- 完成 231 项全量回归、Python 编译、前端语法、依赖、计划上下文、文档索引和差异检查。
- 完成交付复盘，记录代码主题误判、中文字体渲染和 Word 空白尾页三项实施证据。
- 在 `person` 目录生成第 08 篇公众号 Word 文章，正文直接包含 GitHub 完整地址。
- 对隐私清理后的 6 页文章重新逐页检查，并通过可访问性、标题、分节、图片和表格几何审计。
- 将设计、实施计划、测试计划和两份 Checklist 状态同步为 completed。

### 关联 Commit

- `feat(code-practice): 完成有界代码实践交付`

### 备注

方案 A 全部完成：本地执行器采用纵深限制但不是强隔离沙箱；公网或多用户环境应替换为容器、虚拟机或专用判题服务。

## 10:31 工作记录

### 完成事项

- 修复讲解正文出现“函数”时误触发代码练习的问题。
- 将代码实践适用性收紧为只检查用户显式学习主题，练习模板选择也不再受模型讲解措辞影响。
- Graph、Web 和代码实践聚焦回归 47 项通过。

### 关联 Commit

- `fix(code-practice): 收紧代码主题判定`

### 备注

问题在功能分支集成测试中发现、尚未进入远端发布；属于一次性实现修正，无需建立独立 Bug 知识库记录。

## 10:28 工作记录

### 完成事项

- 将代码练习生成、结构化 interrupt 和基于真实测试的确定性评价接入 LangGraph 节点与补救路由。
- 新增 State、会话 JSON 与 `code_practice` SSE 事件，并使用公开练习投影阻止隐藏测试下发。
- Web 增加 Starter Code、测试结果、错误类型、三级提示与本地执行安全说明。
- 非代码主题和工具预算为零时继续使用原 LCEL 练习与模型评价。
- 完成 Graph、Routing、Web、代码实践、Context 和 Middleware 共 80 项回归，Python 编译与前端语法检查通过。

### 关联 Commit

- `feat(code-practice): 接入学习流程与 web`

### 备注

Auto-committed by /tdd phase-commit, plan: tool-calling-react-code-practice

## 10:07 工作记录

### 完成事项

- 将语法、策略、超时、资源、运行时和断言失败转换为稳定错误枚举及安全测试明细。
- 按通过测试比例生成确定性分数，隐藏测试不返回输入和期望值。
- 针对各错误类型生成方向提示、定位线索和修复范式三级提示，不返回完整参考实现。
- 完成 17 项代码实践测试及 Context/Middleware 相邻回归，共 47 项通过。

### 关联 Commit

- `feat(code-practice): 错误分类与分级提示`

### 备注

Auto-committed by /tdd phase-commit, plan: tool-calling-react-code-practice

## 02:09 工作记录

### 完成事项

- 实现 AST 默认拒绝策略，禁止 import、动态执行、文件访问、输出、dunder 和私有属性等危险能力。
- 实现一次性临时目录、`-I -S` 隔离模式、最小环境、墙钟/CPU/内存/文件/进程限制的 Python 测试执行器。
- 实现动态工具验证、有界调用、重复 Action 拒绝和安全 Observation 轨迹的 ReAct 控制器。
- 完成 12 项代码实践测试及 Context/Middleware 相邻回归、Python 编译，共 42 项通过。

### 关联 Commit

- `feat(code-practice): 受限执行与有界 react`

### 备注

Auto-committed by /tdd phase-commit, plan: tool-calling-react-code-practice

## 02:05 工作记录

### 完成事项

- 定义代码练习、测试用例、执行报告、错误类型、分级提示和 ReAct 工具轨迹 Schema。
- 实现确定性 Python 函数练习生成器，稳定生成练习 ID、Starter Code 和服务端测试。
- 实现带 Pydantic 参数 Schema 的 LangChain Tool 注册表，按生成/评价阶段和预算开放最小工具集合。
- 完成 5 项代码实践聚焦测试及模型、Runnable 相邻回归，共 33 项通过。

### 关联 Commit

- `feat(code-practice): 工具 schema 与动态注册表`

### 备注

Auto-committed by /tdd phase-commit, plan: tool-calling-react-code-practice

## 02:03 工作记录

### 完成事项

- 完成 Tool Calling、ReAct 与代码实践设计文档、实施计划、单元测试计划及两份可执行 Checklist。
- 锁定本地受限 Python 执行、结构化工具 Schema、动态工具选择、有界 ReAct、确定性评分和三级提示方案。
- 明确本地执行器不是强隔离沙箱，非代码主题和工具预算为零时保留原文本练习路径。
- 计划上下文校验通过并切换为 active。

### 关联 Commit

- `docs(code-practice): 定义有界代码实践交付计划`

### 备注

方案 A：不引入 Docker 或外部执行服务，所有工具循环与代码运行均有硬上限。

## 01:34 工作记录

### 完成事项

- 完成 README 的 GraphRAG 使用说明、资源上限、隐私边界、降级行为和 Web `knowledge_graph` 事件文档。
- 增加公开文档契约测试，并完成 208 项全量回归、Python 编译、依赖、前端语法、计划上下文、文档索引和差异检查。
- 完成交付复盘，记录图报告跨层传播、混合语言句界和文档索引模板噪声。
- 在 `person` 目录生成第 07 篇公众号 Word 文章，正文直接包含 GitHub 完整地址。
- 对 8 页文章完成逐页渲染检查，并通过可访问性、标题、分节、图片和表格几何审计。
- 将设计、实施计划、测试计划和两份 Checklist 状态同步为 completed。

### 关联 Commit

- `feat(graphrag): 完成知识前置图交付`

### 备注

方案 A 全部完成：默认确定性离线抽取，结构化模型增强仅显式注入；不引入图数据库或新增环境变量。

## 00:55 工作记录

### 完成事项

- 完成 GraphRAG 与知识前置图的设计文档、实施计划、测试计划和可执行上下文声明。
- 明确默认离线实体关系抽取、显式可注入结构化模型增强、确定性别名消歧和有界 BFS。
- 锁定图与 Hybrid RAG 的 RRF 融合、前置知识解释、Web 概念图和无图安全降级边界。
- 明确概念图是运行时派生状态，不回写原始资料、Chunk 或增量索引。
- 使用项目虚拟环境验证计划上下文通过，并完成差异格式检查。

### 关联 Commit

- `docs(graphrag): 定义知识前置图交付计划`

### 备注

方案 A：默认确定性离线抽取，结构化模型抽取器仅由调用方显式注入。

## 01:01 工作记录

### 完成事项

- 定义有界概念节点、关系、前置路径和 GraphRAG 报告 Schema，并保持旧教学结果兼容。
- 实现中英文前置、组成和关联关系抽取，支持 Markdown 标题与代码标识符。
- 实现 Unicode/标识符规范化、显式别名归并与不确定同名隔离。
- 增加可注入结构化模型抽取器，非法输出、未知 Chunk 和模型异常均安全降级。
- 完成 9 项 GraphRAG 聚焦测试及 32 项 Hybrid RAG 回归、Python 编译和差异检查。

### 关联 Commit

- `feat(graphrag): 图 schema 与实体消歧`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph

## 01:13 工作记录

### 完成事项

- LCEL Prompt 增加独立前置知识建议，Agent 资料工具同步返回图路径理由。
- 流式与非流式 GroundedTeaching 保留同一 GraphRAG 报告和最终融合来源。
- 将图报告接入 LangGraph State、暂停恢复、`knowledge_graph` 事件、SSE 与 Web 会话 API。
- Web 新增概念节点、带方向关系、前置路径、补课原因和来源位置展示，无有效图时自动隐藏。
- 动态内容统一通过 `textContent` 创建，并补充移动端单列布局。
- 完成 79 项 LCEL、Middleware、Graph、Web、检索和 GraphRAG 相邻测试及前端语法检查。

### 关联 Commit

- `feat(graphrag): 接入教学流程与 web`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph

## 01:09 工作记录

### 完成事项

- 按图距离、路径置信度和查询匹配对关系证据 Chunk 进行稳定排名。
- 使用 RRF 融合 Hybrid 与图排名，保留关键词、向量、重排、图分和最终融合分追溯。
- 实现 GraphStudyRetriever，先执行原自校正检索，再做图增强；无图或异常时保持 Hybrid 结果。
- 默认检索适配器启用离线 GraphRAG，仍允许注入旧 Hybrid Retriever。
- 旧粘贴文本的公开 Chunk ID 同步映射到来源、概念节点、关系与前置解释。
- 完成 64 项 GraphRAG、Hybrid RAG 和兼容检索测试。

### 关联 Commit

- `feat(graphrag): 融合图与 hybrid 检索`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph

## 01:05 工作记录

### 完成事项

- 构建稳定、去重且有边界的会话级概念图，合并关系证据与最高置信度并过滤自环。
- 从查询、学习薄弱信息和 Hybrid 命中 Chunk 识别图遍历种子。
- 实现深度 3、访问 24 节点、最多 5 路径的反向前置 BFS，覆盖链、分支与环。
- 生成包含目标、前置路径、薄弱点匹配、Chunk ID 和安全来源位置的补课解释。
- 完成 16 项 GraphRAG 聚焦测试及摄取、Hybrid RAG 相邻回归，共 60 项通过。

### 关联 Commit

- `feat(graphrag): 概念图构建与前置遍历`

### 备注

Auto-committed by /tdd phase-commit, plan: graphrag-prerequisite-graph

## 19:40 工作记录

### 完成事项

- 定义第 09 阶段"LangGraph 状态图进阶"设计：Reducer 增量合并、Command 并行 fan-out 与条件导航、节点级 Retry、诊断节点 Cache 和不变的循环终止边界。
- 创建实施计划、实施 Checklist、单元测试计划与测试 Checklist，并通过计划上下文校验。
- 更新 spec 与 plan 索引，创建功能分支 codex/langgraph-advanced-state-0815。

### 关联 Commit

- `docs(langgraph): 定义状态图进阶交付计划`

### 备注

设计决策 D3/D4：只缓存纯函数化的 make_diagnostic；缓存回放不能携带首个会话的 runtime 上下文，因此诊断节点瘦身为只输出诊断字段。

## 20:05 工作记录

### 完成事项

- 为 `recent_errors` 定义增量合并 Reducer，新增 `learning_events` 有界追加 Reducer、`practice_kind` 字段和 `LearningEvent` Schema。
- 新增确定性 `prepare_practice` 节点：只依赖主题与工具预算判定练习类型并预生成代码练习，可与讲解并行。
- `collect_diagnostic` 返回 Command 双分支 fan-out；`assess` 返回 Command 条件导航并只提交错误增量；`make_quiz` 改为 fan-in 汇合点并保留直调兼容。
- 诊断节点瘦身为只输出诊断字段；补充 Reducer、Schema、并行事件、代码并行准备与两次失败后终止的图级测试。
- 全量回归 242 项通过。

### 关联 Commit

- `feat(langgraph): reducer 合并与并行练习准备`

### 备注

缓存回放不能携带首个会话的 learning_goal，因此该字段改由 CLI/Web 入口显式写入初始 State。

## 20:32 工作记录

### 完成事项

- 新增 resilience 模块：瞬态错误分类（内置超时/连接错误与 Provider 类名白名单）、默认有界 RetryPolicy（每节点最多 2 次尝试）和诊断缓存键。
- 五个模型节点挂接可注入的 RetryPolicy；收集节点与确定性节点不重试。
- 图编译接入 InMemoryCache 与 GRAPH_NODE_CACHE 开关（默认开启），纯函数化的 make_diagnostic 按"主题 + 题图指纹"复用结果。
- 新增 test_resilience.py 共 11 项：瞬态分类、快速失败、尝试上限、缓存命中/不命中/关闭/键覆盖，全量回归 253 项通过。

### 关联 Commit

- `feat(langgraph): 节点级重试与诊断缓存`

### 备注

重试与 LCEL 备用链叠加的最坏情况为每节点 4 次模型调用，仍处于 CONTEXT_MODEL_CALL_LIMIT 预算语义之外的单节点尝试上限内。

## 20:55 工作记录

### 完成事项

- SessionView 暴露 practice_kind 与 learning_events，会话 JSON 与等待/完成两个视图均回填并行轨迹。
- Web 页面动态上下文面板显示练习类型与最近 6 条并行执行轨迹，明确标注并行顺序不保证。
- 端到端图测试覆盖三次 interrupt 恢复、两次失败后进入总结、并行事件合并与隐藏测试不下发。
- 全量回归与前端语法检查通过。

### 关联 Commit

- `feat(langgraph): 接入 web 并展示并行轨迹`
