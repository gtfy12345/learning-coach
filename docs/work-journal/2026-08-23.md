# 2026-08-23 工作日志

## 11:30 工作记录

### 完成事项

- 教学流程要点覆盖改造：新增 `break_down_topic` 节点，会话开始先把学习主题拆解为 1-5 个要点（结构化输出 `TopicPoints`，失败确定性退化为单要点），接入 `recall_memory` 之后的路由，并复用诊断缓存策略按主题+图片缓存。
- 讲解覆盖契约：LCEL 与 Agent 两条讲解路径的 Prompt 均要求逐一覆盖主题要点（已掌握要点简要巩固），长度上限按要点数量自适应（每要点约 300-400 字），替换原先"针对薄弱点、一个场景、300 字"的契约；`TeachingContext` 增加 `topic_points` / `mastered_points` 投影。
- 多缺口评价：`Assessment` 新增 `point_results`（按要点逐项 `mastered`/`gap`，保留 `missing_point` 兼容字段），理解检查与练习评价都会传入要点清单并回写 `mastered_points`，补讲聚焦未掌握要点。
- 练习题绑定要点：出题 Prompt 明确"题目必须考察刚才讲解覆盖的要点"，讲解要点随出题输入传入；quiz/assessment 链增加输入适配器，要点缺省时按"未拆解要点"渲染，不破坏直接调用方。
- 历史问题记录与页面：创建会话时按学习者标识把主题/目标/模式/时间写入 Store `question_history` 命名空间（纳秒键保证同秒内最新在前，默认保留 50 条，存储异常不阻断会话创建）；新增 `GET /api/learners/{learner_id}/questions`（仅本机）与 `/history` 页面（按学习者标识查询），首页与设置页增加导航入口。
- 本机启用 SQLite 持久化：新增 `.env` 配置 `CHECKPOINT_DB_PATH=data/checkpoints.db` 与 `MEMORY_DB_PATH=data/memory.db`（`*.db` 已在 .gitignore），服务重启后检查点、长期记忆、课程进度与历史问题跨重启保留。
- 测试隔离加固：新增 `tests/conftest.py` 自动清除两个 DB 路径环境变量，防止开发机 `.env` 通过 `load_dotenv()` 渗入测试进程、把无关测试接到共享 SQLite 文件。
- 完整测试套件 390 项全部通过；`git diff --check` 干净；服务带 SQLite 重启后 `/history`、查询 API 与空态渲染浏览器实测通过。

### 关联 Commit

- 待提交：`feat(teaching): 主题要点覆盖教学与历史问题页`

### 备注

- 服务重启后页面配置的 API Key 按设计清空，需在设置页重新配置；创建会话在模型未配置时拒绝，不会写入历史问题（记录只跟随真实会话创建）。
- 早前同日修复：`/static` 响应增加 `Cache-Control: no-cache` 并给静态引用加版本参数，解决 WebKit 启发式缓存导致设置页模型清单不更新；设置页模型建议改为始终可见的可点击按钮（WebKit datalist 无点击下拉）。

## 15:20 工作记录

### 完成事项

- 设置页模型配置可选持久化：新增「同时保存到本机 .env」复选框（默认勾选），应用已测试配置时把模型选择与该厂商 Key/Base URL 写入本机 `.env`（保留原有行、0600 权限、.gitignore 内），响应仅回写变量名不回显密钥。
- 按厂商环境变量：新增 `ZHIPU_API_KEY` / `DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY` / `OPENAI_COMPATIBLE_API_KEY` 与对应 `{PROVIDER}_BASE_URL`；启动时按厂商读取注入模型创建，OpenAI 兼容通道仍回退 `OPENAI_API_KEY`。
- `_ensure_runtime` 包裹模型工厂异常并透传原始信息：`.env` 配了模型但缺 Key 时返回"未配置 + 原因"而不是 500。
- 新增 5 项测试（环境变量映射、导出映射、持久化写入/不写入、套件合并 Base URL、设置页契约），完整套件 433 项通过；README 与 .env.example 同步新的持久化边界描述。

### 关联 Commit

- 待提交：`feat(web): 设置页模型配置可选写入本机 .env`

### 备注

- 页面写入只更新被应用的键，不清除其他厂商的历史变量；CLI 模式持久化只写模型 ID 不写凭据。
