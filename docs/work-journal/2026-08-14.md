# 2026-08-14 工作日志

## 18:46 工作记录

### 完成事项

- 完成多模态资料摄取的最终安全复查：网页正文改为流式限额读取，补充 Windows 路径清洗、URI 协议约束和公开哈希脱敏。
- 完成 148 项完整测试、Python 编译、依赖一致性、前端语法、计划上下文、文档索引和差异检查。
- 完成交付复盘，记录新状态消费者遗漏与网络响应提前终止两类可复用防线。
- 生成第 05 篇公众号 Word 文章，完成 8 页逐页视觉检查、表格几何和可访问性审计。
- 将设计、实施计划与测试计划状态统一收口为 completed。

### 关联 Commit

- `feat(ingestion): 完成多模态资料摄取交付`

### 备注

方案 A：会话级内存增量索引，不引入数据库或持久化向量库。

## 17:28 工作记录

### 完成事项

- 完成多模态资料摄取设计、实施计划、测试计划与可执行上下文声明。
- 定义有界 MaterialInput、统一 Metadata、摄取报告和 Loader Registry。
- 实现 PDF、DOCX、PPTX、EPUB、HTML、文本、Markdown 与代码 Loader，并保留页码、幻灯片、章节、标题和代码行号。
- 实现带 SSRF 防护与重定向复检的网页 Loader，以及一次调用上限的视觉图片 Loader。
- 完成 18 项 Loader 聚焦测试、Python 编译和差异检查。

### 关联 Commit

- `feat(ingestion): loader 与统一 document 契约`

### 备注

Auto-committed by /tdd phase-commit, plan: multimodal-material-ingestion

## 20:22 工作记录

### 完成事项

- 完成自校正 Hybrid RAG 的设计、实施计划、测试计划与可执行上下文声明。
- 增加本地确定性哈希 Embedding、归一化向量和 2,048 项有界文档向量缓存。
- 支持显式 LangChain Provider Embedding，默认模式不需要 API Key 或网络。
- 定义检索分数、两次尝试上限和安全追溯报告，并保持旧 StudySource 兼容。
- 完成 9 项新增聚焦测试及 28 项相邻回归、Python 编译和差异检查。

### 关联 Commit

- `feat(rag): embedding 与检索契约`

### 备注

Auto-committed by /tdd phase-commit, plan: self-corrective-hybrid-rag

## 20:26 工作记录

### 完成事项

- 实现带中英文与代码词元支持的内存 BM25 关键词召回和语料级长度归一化。
- 实现带 Chunk Hash 缓存的 Dense 余弦召回，异常、维度错误和非有限值安全降级。
- 实现基于排名的 RRF 去重融合，避免直接混合 BM25 与向量原始分数。
- 实现结合查询覆盖、短语命中和通道一致性的确定性二阶段重排。
- 完成 28 项 Hybrid RAG 聚焦及旧检索回归、Python 编译和差异检查。

### 关联 Commit

- `feat(rag): 双路召回融合与重排`

### 备注

Auto-committed by /tdd phase-commit, plan: self-corrective-hybrid-rag

## 18:24 工作记录

### 完成事项

- 增加统一 MaterialIngestionPipeline，并为 CLI 接入可重复 `--material PATH_OR_URL`。
- Web JSON/SSE 会话支持多文件、换行网页 URL 和安全摄取报告，失败时不创建半成品会话。
- 浏览器增加资料多选、网页输入、摄取统计及文件名与页码/行号来源展示。
- 保留诊断 `--image` 与学习资料图片的独立语义，并兼容旧 study_material 字段。
- 修正动态 Context 对新 study_chunks 的识别，使 Agent 能开放资料检索工具。
- 完成 106 项相邻回归、Python 编译、前端语法和差异检查。

### 关联 Commit

- `feat(ingestion): 接入 cli 与 web 摄取`

### 备注

Auto-committed by /tdd phase-commit, plan: multimodal-material-ingestion

## 17:34 工作记录

### 完成事项

- 实现位置感知 Splitter，在页、章节、标题和代码行单元内生成稳定有界 Chunk。
- 为来源、原始内容和 Chunk 增加 SHA-256，并保持重复执行结果一致。
- 实现会话级内存增量索引，覆盖 added、updated、skipped、deleted 和 full cleanup。
- 将新 Chunk 接入现有词法检索、LCEL 与 Middleware，同时保留旧纯文本资料兼容路径。
- 扩展 StudySource、LearningState 和来源格式，返回文件名、URL、类型、位置和 Chunk Hash。
- 完成 58 项相邻回归、Python 编译和差异检查。

### 关联 Commit

- `feat(ingestion): splitter 与增量索引`

### 备注

Auto-committed by /tdd phase-commit, plan: multimodal-material-ingestion
