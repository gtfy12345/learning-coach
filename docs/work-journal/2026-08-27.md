# 2026-08-27 工作日志

## 交付：macOS 桌面应用

### 完成事项

- 桌面入口模块 `src/learning_coach/desktop.py`：数据目录解析（`LEARNING_COACH_DATA_HOME` 可覆盖，macOS 默认 `~/Library/Application Support/LearningCoach`）、加载数据目录 `.env`、未显式配置时默认把 `CHECKPOINT_DB_PATH`/`MEMORY_DB_PATH` 落到目录内 SQLite、PATH 补齐（`/opt/homebrew/bin` 等，GUI 进程最小 PATH 下也能找到 codex/claude CLI）、flock 单实例锁、回环空闲端口、uvicorn 后台线程、`/api/health` 就绪轮询、pywebview 原生窗口（懒加载，无头环境可导入）。
- CLI 新增 `desktop` 子命令（`--headless`/`--port`/`--data-home`），非零退出码才转 `SystemExit`；现有 `web`/CLI 行为零改动，桌面模式通过 `create_app(service=LearningSessionService(env_file=...))` 注入 `.env` 路径，未改 web.py/model_config.py。
- 打包链路：`packaging/entry.py` 冻结入口、`packaging/LearningCoach.spec`（`collect_all` 显式收集 init_chat_model 动态导入的 langchain provider 集成、tiktoken 数据、uvicorn 协议、webview 平台后端；`collect_data_files` 打入 static）、`scripts/make_app_icon.py`（Pillow + iconutil 生成图标，产物不入库）、`scripts/build_macos_app.sh`（测试→图标→打包→ad-hoc 签名校验）。
- 新增 `tests/test_desktop.py` 14 项测试（目录解析、env 引导与显式配置优先、PATH 幂等、单实例锁互斥、端口可用性、健康轮询、真实 uvicorn 线程停机、CLI 分发、空参数校验），全部无 GUI、无真实模型。
- 顺手修复既有缺陷 `tests/test_model.py::test_create_model_suite_reuses_matching_fallback_model_id`：`create_model_suite()` 内部 `load_dotenv()` 会读入本机 `.env` 的 Provider Key 使 `api_keys` 非空，fake 不接受该参数而失败；monkeypatch `provider_env_credentials` 恢复测试密闭性（与本交付无因果，在未修改代码上同样失败）。
- 冒烟验证：未打包 headless 模式 health 200、SIGINT 优雅退出 exit 0；打包产物 health 200、数据目录正确创建（checkpoints.db/memory.db/app.lock）、单实例锁在真实双启动下正确拒绝并提示；windowed 冻结包忽略 SIGINT 属 PyInstaller bootloader 行为，GUI 主路径（关窗退出）不受影响，冒烟用 SIGTERM。
- 文档：README 新增「macOS 桌面应用」章节与边界两条、项目结构补 packaging/scripts/desktop；`.env.example` 说明桌面数据目录；新增 spec `macos-desktop-app-design.md` 与 plan `macos-desktop-app/`（implementation/checklist/context.yaml），两个 INDEX 已更新。

### 关联 Commit

- 待提交：`feat(desktop): 打包 macOS 桌面应用 LearningCoach.app`

### 备注

- 构建产物 91MB（langchain 全家桶，属预期）；仅本机 ad-hoc 签名与单架构（arm64），正式分发需另行签名公证。
- 依赖变化：requirements.txt 新增 `pywebview>=5.0`（仅 desktop 路径导入，macOS 带 pyobjc）；requirements-dev.txt 新增 `pyinstaller>=6.0`。
- 课程正文缓存仍是进程内存（重启需重新上传），维持既有边界。
