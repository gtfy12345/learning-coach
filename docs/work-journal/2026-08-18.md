# 2026-08-18 工作日志

## 21:40 工作记录

### 完成事项

- 优化模型设置页：修复 Base URL 输入框缺失样式的问题，Provider 展示卡改为可点击快捷选择，模型名按 Provider 记忆并接入推荐模型联想。
- 测试票据增加倒计时与过期自动失效，API Key 支持显示/隐藏切换，Base URL 提前做 HTTPS 校验提示，测试按钮忙碌态与 CLI 反馈文案改进。
- 379 项测试中新增设置页契约断言；浏览器实测桌面与移动端布局、快捷选择、模型名记忆、密钥显隐与校验路径全部通过。

### 关联 Commit

- `feat(web): 优化模型设置页交互`

### 备注

- 8010 端口验证实例与 8000 端口用户实例互不影响；页面 API Key 仍仅存进程内存。

## 23:10 工作记录

### 完成事项

- 新增资料课程学习：整本资料按真实结构确定性拆章（EPUB 章节、DOCX/HTML 标题、PDF 目录优先、页码/片段分段兜底），章节过短合并、上限 60 章，零模型调用。
- 课程服务与四个 API（建课、课程列表、课程详情、章节会话 SSE）；建课路径放宽单文件 50 MiB/100 万字符，普通会话上限不变；书正文只进进程内存缓存（≤3 本），章节进度写入长期记忆同款 Store，重传同文件按内容哈希挂回课程。
- 每章复用现有先教后测学习图，会话创建时按章预过滤片段；会话完成自动写回章节进度；PDF loader 增加 best-effort 目录提取并跳过 EPUB 导航页。
- 首页改双入口 tab（主题学习/资料课程），课程卡片、章节进度徽标、返回课程与完成后进度刷新；README、.env.example 同步。
- 全量 379 项测试通过（新增 18 项，全部 fake 模型）；浏览器实测真实 EPUB 建课、章节列表与双端布局；章节会话完整流程由 API 测试覆盖，浏览器端未触发真实模型调用。

### 关联 Commit

- `feat(course): 支持整本资料拆章课程学习`

### 备注

- CLI 仍只有 teach_first/diagnose_first；课程进度默认内存存储，配置 MEMORY_DB_PATH 后跨重启保留。
